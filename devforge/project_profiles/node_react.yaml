profile: node_react
validation:
  commands:
    install_check: "node --version && npm --version"
    lint: "npm run lint"
    typecheck: "npm run typecheck"
    test: "npm test"
    build: "npm run build"
